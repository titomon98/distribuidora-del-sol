# CRA incrusta esto al compilar (npm run build).
# Ruta relativa: el frontend llama /api y /socket.io en el MISMO origen que sirve Nginx,
# así no hay problemas de CORS ni IPs quemadas. No cambiar salvo que separes dominios.
REACT_APP_API_URL=/api
